import { messagesEnGb as AudioBrowser } from 'AudioBrowser/language'; //Special for NDLA
import { messagesEnGb as LicenseChooser } from 'LicenseChooser';
import { messagesEnGb as LicenseIcon } from 'LicenseIcon';
import { messagesEnGb as LicenseText } from 'LicenseText';
import { messagesEnGb as Owner } from 'Owner';
import { messagesEnGb as QuestionContentType } from 'QuestionContentType';
import { messagesEnGb as TagsManager } from 'TagsManager';
import { messagesEnGb as VideoBrowser } from 'VideoBrowser/language'; //Special for NDLA
import { messagesEnGb as EmbedContentType } from 'EmbedContentType';
import { messagesEnGb as H5pEditor } from 'H5pEditor';
import { messagesEnGb as FileUploadProgress } from 'FileUploadProgress';
import { messagesEnGb as Sidebar } from 'Sidebar';
import { messagesEnGb as Article } from 'Article';

export default {
    locale: 'en-GB',
    messages: Object.assign(
        {},
        AudioBrowser,
        LicenseChooser,
        LicenseIcon,
        LicenseText,
        Owner,
        QuestionContentType,
        TagsManager,
        VideoBrowser,
        EmbedContentType,
        H5pEditor,
        FileUploadProgress,
        Sidebar,
        Article
    ),
};
