<iframe
    src="{{ $src }}"
    title="{{ $title }}"
    width="800"
    height="600"
    allowfullscreen="allowfullscreen"
    allow="fullscreen *; geolocation *; microphone *; camera *; midi *; encrypted-media *"
></iframe>
